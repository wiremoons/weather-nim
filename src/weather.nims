# see https://nim-lang.org/docs/nims.html
# build with SSL library support
switch("define", "ssl")
# build with the clang compiler (not GCC)
#switch("cc", "clang")
# increase build compiler outputs
#switch("verbosity", "2")
